
import './index.scss';

const Sidebar = (props: any) => {

    return (
        <section className="sidebar">
            <ul className="sidebar__list">
                <li className="sidebar__list__item">Home</li>
            </ul>
        </section>
    );
};

export default Sidebar;