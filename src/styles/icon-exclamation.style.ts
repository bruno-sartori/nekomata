import { css } from "lit";

export const iconExclamationStyle = css`
  .icon-exclamation {
    fill: var(--secondary);
  }

  .icon-exclamation:hover {
    fill: var(--primary);
    cursor: pointer;
  }
`;
