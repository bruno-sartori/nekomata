import { css } from "lit";

export const iconBurguerStyle = css`
  .icon-burguer {
    cursor: pointer;
  }
  
  .icon-burguer__content {
    fill: var(--text-tertiary);
  }

  .icon-burguer__hover {
    fill: var(--text-primary);
  }
`;
