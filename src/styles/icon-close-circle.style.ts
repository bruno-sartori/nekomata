import { css } from "lit";

export const iconCloseCircleStyle = css`
  .icon-close-circle {
    fill: var(--icon-primary);
  }

  .icon-close-circle:hover {
    fill: var(--error);
    cursor: pointer;
  }
`;
